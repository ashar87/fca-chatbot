// Shared portal section type — used by Sidebar, ChatWidget, and page.tsx
export type PortalSection = "nsm-search" | "nsm-about" | "firds" | "fitrs" | "short-selling";

// Legacy alias kept for any remaining imports
export type PortalTab = PortalSection;
